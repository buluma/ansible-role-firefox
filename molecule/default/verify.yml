---
- name: Verify
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: check firefox version installed
      ansible.builtin.debug:
        msg: "Installed firefox version successfully!"
